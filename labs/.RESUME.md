# Lab build session — resume notes

## Status at shutdown

Six background agents were dispatched in parallel to scaffold the coming-soon labs (excluding Co-Pilot Lab per user instruction). **All confirmed failures hit the same wall:** Write permission denied for new files under `labs/`. Each agent had the full ~700–1500-line file content composed, but the sandbox blocked the Write call.

| Lab | Agent state | Lines prepared | Modes scoped |
|---|---|---|---|
| 📡 IR Lab — `labs/ir-lab.html` | ❌ Blocked (Write denied) | ~870 | 5 (Live Readings · Threshold Tuner · Auto Line Follow · Calibration · Decision Sandbox) |
| 👀 Vision Lab — `labs/vision-lab.html` | ❌ Blocked (Write denied) | ~605 | 5 (Color Zone · Light Sensor · Face Steer · QR Cards · Hand Wave) |
| 🎵 Music Lab — `labs/music-lab.html` | ❌ Blocked (Write denied) | ~990 | 5 (Piano · Step Sequencer · Pre-baked Tunes · Beat→Motion · Tempo Match) |
| 🦾 Servo Lab — `labs/servo-lab.html` | ❌ Blocked (Write denied) | ~795 | 5 (Live Trim · Sweep · Keyframe · Scenes · Trigger Mapper) |
| 🪄 Lights Lab — `labs/lights-lab.html` | ❌ Blocked (Write denied) | ~752 | 7 (6 mood scenes + 8-step painter; Tier-1 preview, Tier-2 throttled live) |
| 📏 Distance Lab — `labs/distance-lab.html` | ⏳ Still running at shutdown | — | 8 of 11 (Sonar · Tape Measure · Parking · Tripwire · Hot/Cold · Theremin · Cruise · Follow-Me) |

The Co-Pilot Lab card was deliberately excluded.

## Resume recipe

The path forward is **re-dispatch the failures with `isolation: "worktree"`** so each agent gets its own git worktree where Write is permitted. The completed files come back automatically when the worktree merges or via a copy step.

Suggested prompt template per agent (one-line change from the original):

```
Same instructions as before, but you'll be given a clean isolated git worktree —
use the Write tool to create labs/<name>-lab.html in that worktree. Output
content end-to-end, no truncation.
```

Original prompts for each agent are recoverable from the conversation transcript
under the "coming soon: start agents" turn.

## Already shipped this session (no resume needed)

- `labs/index.html` — 8 cards (1 live + 7 coming), full theme + lang pickers, 12 i18n keys total per language including all card_*_h/p/tag/use entries; new wishlist link in footer
- `labs/wishlist.html` — 27-idea voting page with localStorage 👍 + ⭐ (cap 3, FIFO), `mailto:contact@workshop-diy.org` send-picks button, GitHub-issue fallback, full trilingual T-table
- `labs/joystick-lab.html` — added theme picker (was missing), wired to shared `robi.theme` localStorage key
- `workshops/theme.css` — added `.lang-pills` + `.lang-pill` + 3 inline-SVG `.flag-*` classes (UK / France / **Algerian** for AR)
- All 25 HTML files in docs/labs/workshops — converted from emoji-flag UIs to SVG flag pills (4 button-pill files, 3 redirect-link files via manual.* trilogy, 6 in-place pill upgrades, 12 docs link-list conversions)
- Distance Lab card + Lights Lab card + Co-Pilot Lab card stubs added to `labs/index.html` (all coming-soon, all trilingual)
- Co-Pilot Lab also added to `labs/wishlist.html` Build cluster
- `index.html` (main app) — 🧪 Labs FAB at bottom-center next to Connect / STOP

## Cards-to-flip-when-labs-land

When each lab file lands, in `labs/index.html` for that card:
1. Remove the `card coming` class → just `card`
2. Change the wrapping `<div>` to `<a class="card" href="<lab>-lab.html">`
3. Drop the `coming-soon` badge (the `.card.coming::after` rule auto-hides when class is removed)

## Open product threads (not started)

- 🛰 **Co-Pilot Lab** itself — the foundation lab (pairing wizard for 2nd micro:bit as radio gateway). Card is in place; lab page deliberately deferred. When ready, this is the prereq that other labs reference for their Tier-2 capabilities (Lights wand, Walkie, Cipher, Worm, Add-on).
- Firmware verb `LIGHT:<scene>` for glitch-free NeoPixel — proposed but not added to `firmware/v1-maqueen-lib.ts`.
- Wand sketch `firmware/wand.ts` — never created.

## Saved on

Resume by reading this file then re-dispatching the 4–6 lab agents with `isolation: "worktree"`. All other state is in the conversation transcript at:
`C:\Users\Win10\.claude\projects\D--work-dev-16-workshop-100-github-apps-repos-tutocast\657727e2-3f5b-4fd2-b36f-e32f7ff5c627.jsonl`
